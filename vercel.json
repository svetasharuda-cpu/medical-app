{
  "buildCommand": "npx expo export -p web && npx workbox generateSW workbox-config.js",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/|sw\\.js$).*)", "destination": "/index.html" }
  ]
}
